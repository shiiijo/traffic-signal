## Steps to run the project locally

1. Clone the repository
2. cd to the repository
3. run ```npm install```
4. run ```npm start```
5. applocation will run locally on port ```3000```

checkout the application locally on ```http://localhost:3000/```
